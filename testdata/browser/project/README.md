# Browser fixture project

This folder exists for the repeatable Web UI feature matrix. It is intentionally
small so sandboxed `read`, `grep`, `find`, and `ls` calls have deterministic
results.

Expected marker: `DIRE_AGENT_BROWSER_PROJECT_OK`.
